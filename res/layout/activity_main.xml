<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />

    <TextView
        android:id="@+id/usbAudioTV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/text"
        android:layout_below="@+id/text"
        android:layout_marginTop="1dp"
        android:text="Usb Audio: " />

    <Button
        android:id="@+id/musicBtn"
        android:layout_width="600px"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="播放音乐" />

    <Button
        android:id="@+id/usbBtn"
        android:layout_width="600px"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/musicBtn"
        android:layout_below="@+id/musicBtn"
        android:layout_marginTop="20dp"
        android:text="支持 USB audio" />

    <Button
        android:id="@+id/unSupportUsbBtn"
        android:layout_width="600px"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/musicBtn"
        android:layout_below="@+id/usbBtn"
        android:layout_marginTop="20dp"
        android:text="不支持 USB audio" />

    <Button
        android:id="@+id/submitBtn"
        android:layout_width="600px"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/musicBtn"
        android:layout_below="@+id/unSupportUsbBtn"
        android:layout_marginTop="20dp"
        android:text="上传" />

</RelativeLayout>